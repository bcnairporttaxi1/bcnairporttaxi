/**
 * @bcn/api-client — placeholder.
 *
 * Typed client for /api/v1. Generated in Phase 1 — intentionally empty.
 *
 * The package exists now so the workspace graph, tooling and CI are already
 * shaped for it. Adding a package later is a bigger change than filling one in.
 */
export {};
